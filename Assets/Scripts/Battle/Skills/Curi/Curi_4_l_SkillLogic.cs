using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Linq;
using Enums;

namespace Battle.Skills{

public class Curi_4_l_SkillLogic : BaseActiveSkillLogic {

        /*public override void ActionBeforeMainCasting(Casting casting){
            // 랜덤한 속성 하나를 정한다
            Element randomElement = (Element)Random.Range(0, 5);
            // casting의 모든 타겟의 속성을 위에서 지정한 랜덤한 속성으로 교체한다
            List<Unit> targets = casting.GetTargets();
            targets.ForEach(target => {target.myInfo.element = randomElement;});
        }*/
    }
}

